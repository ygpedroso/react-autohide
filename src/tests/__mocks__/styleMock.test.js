import styleMock from './styleMock';

describe('Suite of tests for the Style Mock object', () => {
    test('Test that styleMock exports correct value', () => {
        expect(styleMock).toEqual({});
    });
});
